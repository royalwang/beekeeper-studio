import { createSlice } from '@reduxjs/toolkit';

const initialState = {
  // Add search-related state here
};

const searchSlice = createSlice({
  name: 'search',
  initialState,
  reducers: {
    // Add search-related reducers here
  },
});

export default searchSlice.reducer;
