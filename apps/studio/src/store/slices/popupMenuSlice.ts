import { createSlice } from '@reduxjs/toolkit';

const initialState = {
  // Add popup menu-related state here
};

const popupMenuSlice = createSlice({
  name: 'popupMenu',
  initialState,
  reducers: {
    // Add popup menu-related reducers here
  },
});

export default popupMenuSlice.reducer;
