import { createSlice } from '@reduxjs/toolkit';

const initialState = {
  // Add export-related state here
};

const exportsSlice = createSlice({
  name: 'exports',
  initialState,
  reducers: {
    // Add export-related reducers here
  },
});

export default exportsSlice.reducer;
