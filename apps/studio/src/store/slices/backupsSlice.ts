import { createSlice } from '@reduxjs/toolkit';

const initialState = {
  // Add backups-related state here
};

const backupsSlice = createSlice({
  name: 'backups',
  initialState,
  reducers: {
    // Add backups-related reducers here
  },
});

export default backupsSlice.reducer;
