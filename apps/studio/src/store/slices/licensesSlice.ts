import { createSlice } from '@reduxjs/toolkit';

const initialState = {
  // Add licenses-related state here
};

const licensesSlice = createSlice({
  name: 'licenses',
  initialState,
  reducers: {
    // Add licenses-related reducers here
  },
});

export default licensesSlice.reducer;
