import { createSlice } from '@reduxjs/toolkit';

const initialState = {
  // Add hide entities-related state here
};

const hideEntitiesSlice = createSlice({
  name: 'hideEntities',
  initialState,
  reducers: {
    // Add hide entities-related reducers here
  },
});

export default hideEntitiesSlice.reducer;
