import { createSlice } from '@reduxjs/toolkit';

const initialState = {
  // Add menu bar-related state here
};

const menuBarSlice = createSlice({
  name: 'menuBar',
  initialState,
  reducers: {
    // Add menu bar-related reducers here
  },
});

export default menuBarSlice.reducer;
