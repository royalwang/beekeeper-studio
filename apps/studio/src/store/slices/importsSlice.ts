import { createSlice } from '@reduxjs/toolkit';

const initialState = {
  // Add imports-related state here
};

const importsSlice = createSlice({
  name: 'imports',
  initialState,
  reducers: {
    // Add imports-related reducers here
  },
});

export default importsSlice.reducer;
