import { createSlice } from '@reduxjs/toolkit';

const initialState = {
  // Add multi-table exports-related state here
};

const multiTableExportsSlice = createSlice({
  name: 'multiTableExports',
  initialState,
  reducers: {
    // Add multi-table exports-related reducers here
  },
});

export default multiTableExportsSlice.reducer;
